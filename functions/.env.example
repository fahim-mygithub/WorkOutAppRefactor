# Copy to .env for local emulator runs. NEVER commit a real key.
# In production prefer the Secret Manager binding (see README):
#   firebase functions:secrets:set ANTHROPIC_KEY
ANTHROPIC_KEY=sk-ant-...
